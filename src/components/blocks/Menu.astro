---
const { ...props } = Astro.props;
---

<ul class="menu" {...props}>
  <li class="menu__item">
    <a href="/#about-us" class="menu__link active">Компания</a>
  </li>
  <li class="menu__item">
    <a href="/#full-dev-cyrcle" class="menu__link">Полный цикл</a>
  </li>
  <li class="menu__item">
    <a href="/#what-we-do" class="menu__link">Направления работы</a>
  </li>
  <li class="menu__item">
    <a href="/#clients" class="menu__link">Клиенты</a>
  </li>
  <li class="menu__item">
    <a href="/#career" class="menu__link">Карьера</a>
  </li>
  <li class="menu__item">
    <a href="/#contact-form" class="menu__link">Контакты</a>
  </li>
  <li class="menu__item"><slot name="last" /></li>
</ul>

<style>
  @import "/src/assets/css/_custom-media.css";

  .menu {
    padding: 0;
    margin: 0;
    display: flex;
    align-items: baseline;
  }
  .menu__item {
    list-style-type: none;
    padding: 0;
    margin: 0 var(--20px);
    &:empty {
      display: none;
    }
  }
  .menu__link {
    text-decoration: none;
    font-size: var(--text-font-size);
    color: currentColor;
    &.active {
      text-decoration: underline;
      text-underline-offset: var(--8px);
      text-decoration-thickness: var(--2px);
    }
    &:hover {
      text-decoration: underline;
      text-underline-offset: var(--8px);
      text-decoration-thickness: var(--1px);
      cursor: pointer;
    }
  }

  @media screen and (--screen-phone) {
    .menu {
      flex-direction: column;
      gap: var(--24px);
      padding: calc(var(--1px) * 140) var(--30px) var(--110px);
      color: var(--color-product);
      align-items: center;
      position: fixed;
      top: 0;
      left: 0;
      background: rgba(21, 30, 52, 0.8);
      backdrop-filter: blur(var(--110px));
      width: 100%;
      z-index: 5;
      border-radius: 0 0 var(--24px) var(--24px);
      display: none;
      &.active {
        display: flex;
      }
    }
    .menu__link {
      font-size: var(--20px);
    }
  }
</style>
